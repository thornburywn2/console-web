# Console.web Standalone Configuration
# This configuration is for standalone deployments WITHOUT:
# - nginx reverse proxy
# - Authentik SSO
# - Cloudflare Tunnels
#
# Perfect for local development or personal use on Windows WSL2

# Server Configuration
PORT=5275
VITE_PORT=7777
NODE_ENV=production

# Projects Directory (where your code lives)
PROJECTS_DIR=~/Projects

# Database (local PostgreSQL)
DATABASE_URL="postgresql://localhost:5432/console_web?schema=public"

# Authentication - STANDALONE MODE
# When AUTH_ENABLED=false, Console.web uses a simple local password
AUTH_ENABLED=false

# Local password protection (set on first visit)
# Leave empty to be prompted on first run
LOCAL_AUTH_PASSWORD=

# Disable external integrations by default
# These can be enabled later in Settings
GITHUB_ENABLED=false
CLOUDFLARE_ENABLED=false

# AI Features - Code Puppy is enabled by default
CODE_PUPPY_ENABLED=true

# Optional: Anthropic API key for AI features
# Get yours at https://console.anthropic.com/
# ANTHROPIC_API_KEY=

# Logging
LOG_LEVEL=info
